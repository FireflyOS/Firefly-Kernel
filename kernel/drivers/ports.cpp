#include "drivers/ports.hpp"

// inline uint8_t firefly::read_port(uint16_t port) {
//     uint8_t val;
//     asm volatile("inb %1, %0"
//                  : "=a"(val)
//                  : "Nd"(port));
//     return val;
// }

